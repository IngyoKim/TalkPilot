class Project {
  final String title;
  final DateTime createdAt;
  final int practiceCount;

  Project({
    required this.title,
    required this.createdAt,
    required this.practiceCount,
  });
}
